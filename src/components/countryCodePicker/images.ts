const fixedPath = './assets/images'

export default{
    SEARCH_ICON: require(`${fixedPath}/searchPlaceholder.png`),
    CROSS_ICON: require(`${fixedPath}/close.png`),
    REMOVE_ICON: require(`${fixedPath}/remove.png`),
}